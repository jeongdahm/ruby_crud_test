<h2>방명록 게시판</h2>

<table class="table table-striped">
  <thead>
  <tr>
    <th>제목</th>
    <th>내용</th>
    <th>작성시간</th>
  </tr>
  </thead>
  <tbody>
  <% @posts.each do |p| %>

    <tr>
    <td><a href="/post/show/<%= p.id %>" > <%= p.title %> </a></td>
    <td><%= p.content %></td>
    <td><%= p.created_at %></td>
  </tr>

  <% end %>
  </tbody>
</table>

<a href="/">home</a>
<a href="/post/new">write</a>